The scripts in this directory are not meant to be run directly.
They are sourced by ../envtest.sh as needed.

Example usages:

    envcheck.sh -x k8s advana
    envcheck.sh -x k8s sabrscb -w
    envcheck.sh -x k8s smarts -w
