# Docker helpers
# IMPORTANT - this needs to remain bash & zsh compatible

# run a docker-compose command inside the cdx-env-tools/docker/ directory
# (cd'g to the docker/ directory simplifies everything)
dc() (
    set -x
    cd "${CDX_PROJECTS}/cdx-env-tools/docker"
    docker-compose "$@"
)
